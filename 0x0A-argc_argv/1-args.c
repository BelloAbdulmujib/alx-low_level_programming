#include <stdio.h>

/**
 * main - program that prints a number
 * @argc: argument num
 * @argc: argument vector
 * Return: 0 always
 */

int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d\n", argc);
	return (0);
}

#include <stdlib.h>
#include <stdio.h>
/**
 * mul - function that multiply two integers
 * @a:  first parameter
 * @b: second parameter
 * Return: final result
 */

int mul(int a, int b)
{
	return (a * b);
}

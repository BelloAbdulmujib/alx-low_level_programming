#include <stdlib.h>
#include <stdio.h>
/**
 * substraction - function that substract two integers
 * @a:  first parameter
 * @b: second parameter
 * Return: final result
 */

int substract(int a, int b)
{
	return (a - b);
}

#include <stdlib.h>
#include <stdio.h>
/**
 * sub - function that substract two integers
 * @a:  first parameter
 * @b: second parameter
 * Return: final result
 */

int sub(int a, int b)
{
	return (a - b);
}
